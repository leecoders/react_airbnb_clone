import React from "react";
import styled from "styled-components";

const SectionWrapper = styled.div`
  position: relative;
  top: 12.9rem;
  height: 505rem;
  background: #ffffff;
`;

const Section = () => {
  return <SectionWrapper>3213213</SectionWrapper>;
};

export default Section;
